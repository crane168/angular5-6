import { ComponentFactoryResolver, OnDestroy, OnInit, ViewContainerRef } from '@angular/core';
import { DomSanitizer } from '@angular/platform-browser';
import { ConfirmationService } from '../../services/confirmations.service';
import { ConfirmSettings } from '../../interfaces/confirm-settings';
export declare class ConfirmationsComponent implements OnInit, OnDestroy {
    private _service;
    private _resolver;
    private _domSanitize;
    constructor(_service: ConfirmationService, _resolver: ComponentFactoryResolver, _domSanitize: DomSanitizer);
    compViewContainerRef: ViewContainerRef;
    defaultSettings: ConfirmSettings;
    settings: ConfirmSettings | any;
    private _current;
    private _lastResolve;
    private _listener;
    ngOnInit(): void;
    ngOnDestroy(): void;
    private _handleResolve(res?);
    private _buildItemTemplate(key, value);
}
